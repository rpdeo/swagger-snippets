# -*- mode: snippet -*-
# name: swagger-template-head
# key: head
# --
${1:head}:
  summary: ${2:summary}
  description: ${2:description}
  operationId: ${3:headApiCall}
  consumes:
    - "application/json"
  produces:
    - "application/json"
  parameters:
  responses:
    ${4:200}:
      description: ${5:OK}
      schema:
        $ref: "#/definitions/ApiResponse"
${6}